def largest(arr,n):
    max=arr[0]
    for i in range(1,n):
        if arr[i]>max:
            max=arr[i]
    return arr[i]
arr = [11, 22, 33, 56]
n=len(arr)
ans=largest(arr,n)
print("largest element of array",ans)
